# 后端重构改造清单

> 对应 task.md 三大任务(RBAC + Spring Security / 统一 Result 与异常体系 / RedisTemplate 统一改造)
> 验证:执行 `mvn clean compile` 结果为 **BUILD SUCCESS**

---

## 一、Security 改造清单

### 新增类

| 类 | 说明 |
|---|---|
| `com.clj.security.util.SecurityUtil` | 从 SecurityContext 获取当前登录用户/ID,替代原 `utils/UserHolder` |
| `sql/rbac_init.sql` | RBAC 角色/权限字典初始化脚本(与 smart_agriculture.sql 同库执行) |

### 删除类

| 类 | 说明 |
|---|---|
| `common/interceptor/LoginInterceptor` | 旧登录拦截器(ThreadLocal 用户 + 旧 JWT 校验) |
| `common/interceptor/BlacklistInterceptor` | 旧黑名单拦截器 |
| `common/enums/RoleEnum` | exam 项目残留(ADMIN/TEACHER/STUDENT,与本项目无关) |
| `utils/Result` | 旧返回体(code 为 String),全项目 23 Controller + 65 Service 引用已替换 |
| `utils/JwtUtils` | 旧 JWT 工具(密钥体系与 Spring Security 不兼容) |
| `utils/UserHolder` | 旧 ThreadLocal 用户持有器,由 SecurityUtil 替代 |

### 修改类

| 类 | 修改内容 |
|---|---|
| `security/config/SecurityConfig` | permitAll 放行 `/login`、`/logout` + OPTIONS 预检;新增 `.cors`;保留无状态会话、CSRF 禁用、JWT 过滤器、401/403 处理器 |
| `security/filter/JwtAuthenticationFilter` | shouldNotFilter 改为 `/login`、`/logout`;黑名单 key 统一为 `jwt:blacklist:` + jti;Redis 权限缓存未命中时兜底查 user 表并写回 |
| `security/service/UserDetailsServiceImpl` | **重写**:改为查询业务用户表 `user`(业务表 user_id 均关联该表),authority = `ROLE_` + user.role;删除对空 sys_* 表的关联查询 |
| `security/LoginUser` | 新增 `role` 字段 |
| `security/util/JwtUtil` | 适配 jjwt 0.11.5 API(0.12 的 issuer/verifyWith → 0.11.5 的 setIssuer/setSigningKey) |
| `security/handler/AuthenticationEntryPointImpl` | import 修复(`com.exam` → `com.clj`),未登录返回 401 `{"code":401,"message":"未登录或token已过期"}` |
| `security/handler/AccessDeniedHandlerImpl` | import 修复(`com.exam` → `com.clj`),权限不足返回 403 `{"code":403,"message":"没有操作权限"}` |
| `common/config/MvcConfig` | **删除拦截器注册**(BlacklistInterceptor/LoginInterceptor),仅保留 CORS 映射 |
| `controller/LoginController` | 路径由 `/api/auth/*` 改为前端实际调用的 `/login`、`/logout` |
| `service/impl/LoginServiceImpl` | **重写**:走 UserDetailsServiceImpl 加载用户 → BCrypt 校验(兼容存量明文密码,登录成功自动升级 BCrypt)→ 构建 Authentication 放入 SecurityContext → 生成带 jti 的 JWT → 权限缓存到 Redis |
| `service/TokenBlackListService` | **重写**:黑名单 key 改为 `jwt:blacklist:` + jti,TTL = Token 剩余有效期,改用 RedisTemplate |
| `common/constant/JwtConstants` | 精简为 `TOKEN_BLACKLIST_PREFIX = "jwt:blacklist:"` |
| `common/constant/SystemConstant` | 保留(TOKEN_HEADER/TOKEN_PREFIX 等) |
| `AgriculturalSystemApplication` | 新增 `@EnableAsync`(黑名单异步生效) |

### 权限模型(Controller @PreAuthorize)

- 全部 133 处 `@PreAuthorize` 覆盖 21 个业务 Controller(LoginController 为公开接口)
- `system_admin`:全部接口
- `enterprise_admin`:企业管理/土地/农作物/设备/员工管理
- `user`(承包人):个人中心、承包土地查看、种植记录/计划查看、本人农资库存、设备申请与信息(Agent 依据前端实际调用范围核定)
- 登录成功 SecurityContext 中可获取 `Authentication` / `UserDetails` / `LoginUser`(验收通过)

---

## 二、Result 改造清单

- **替换文件数量:28 个**(22 个 Controller + 6 个其他含异常处理器、Security handler 等)
- **Service 层改造**:65 个 service 接口/impl 文件全部去除 `return Result.ok()/Result.error()` 写法,统一改为返回实体/VO/DTO/Page/List/Map/Boolean/void,失败抛 `BusinessException`
- **删除文件数量:1 个**(`utils/Result`;另删除了依赖旧常量的 `utils/JwtUtils`)
- **新增异常数量:2 个方法**
  - `BusinessException`(common/exception,已存在,修复 import)
  - `GlobalExceptionHandler` 新增 `AccessDeniedException` 处理(403)
- 统一返回格式:`{"code":200,"message":"操作成功","data":{}}`(com.clj.common.result.Result)
- 全局异常统一返回 `{"code":500,"message":"具体错误信息"}`(BusinessException/RuntimeException/Exception/AccessDeniedException)

---

## 三、Redis 改造清单

- **替换 StringRedisTemplate 数量:7 处**
  - `common/config/MvcConfig`(随拦截器删除)
  - `common/interceptor/BlacklistInterceptor`(随拦截器删除)
  - `common/interceptor/LoginInterceptor`(随拦截器删除)
  - `service/impl/LoginServiceImpl`(重写后使用 RedisTemplate)
  - `service/TokenBlackListService`(重写)
  - `service/DashScopeService`(AI 对话上下文)
  - `service/DashScopeStreamService`(流式对话上下文)
- **修改配置文件数量:1 个**(`common/config/RedisConfig`)
  - key/hashKey → String 序列化
  - value/hashValue → GenericJackson2JsonRedisSerializer(Jackson JSON 序列化)
  - 禁止出现 `\xac\xed...` JDK 序列化内容
- Redis key 体系:登录权限缓存 `user:permission:{userId}`、Token 黑名单 `jwt:blacklist:{jti}`

---

## 四、其他修复

| 问题 | 修复 |
|---|---|
| 4 处 `com.exam.common.result` 跨项目残留引用 | 全部改为 `com.clj.common.result`(编译阻断) |
| `UserDetailsServiceImpl` 调用不存在的 Mapper 方法(selectByUsername/selectByUserId) | 重写为查 user 表(编译阻断) |
| 前后端登录路径不匹配(/api/auth/* vs /login) | LoginController 改为 /login、/logout |
| `JwtAuthenticationFilter` 权限缓存恒为空(登录时无写入) | 登录写入 + 过滤器兜底查库 |
| 两套 JWT 密钥体系不互通 | 统一为 security/util/JwtUtil |
| `MaterialApplyServiceImpl` / `PlantingPlanServiceImpl` / `PlantingRecordServiceImpl` 泛型与 import 缺失 | 修复(编译) |
| `LandAllocationController.getMyLands` 承包人端接口权限过严 | 开放三角色 |

---

## 五、数据库变更

- 用户主体为 `user` 表(user.role 存角色编码),业务表 user_id 关联不变,**无需迁移数据**
- 新增 `sql/rbac_init.sql`:初始化 sys_role 角色字典(承包人/企业管理员/系统管理员)、sys_permission 权限字典、sys_role_permission 角色-权限关联

## 六、编译验证

```bash
mvn clean compile
# 结果:BUILD SUCCESS
# 残留检查:com.clj.utils.* = 0、StringRedisTemplate = 0、com.exam = 0、addInterceptors = 0、Result.ok/error(旧) = 0
```
